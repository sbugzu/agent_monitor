#!/usr/bin/env python3
"""Compatibility wrapper for the unified Agent Monitor hook installer."""

import os
from pathlib import Path
import sys


installer = Path(__file__).resolve().with_name("install-agent-hooks")
os.execv(
    installer,
    [str(installer), *sys.argv[1:], "codex"],
)
